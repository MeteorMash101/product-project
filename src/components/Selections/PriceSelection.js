import classes from './PriceSelection.module.css';

const PriceSelection = (props) => {
    return (
        <div className={classes.container}>
            Stuff
        </div>
    );
};

export default PriceSelection;