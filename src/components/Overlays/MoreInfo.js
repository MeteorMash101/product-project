import CustomModal from '../../UI/CustomModal.js';

const MoreInfo = (props) => {
  // let content = "";
  // if (props.color === "Red") {
  //   content = 
    
  //   <h2>Red Hot Tomato</h2>
  //   +5 exp Intelligence
  //   +5 exp Strength
  //   +5 exp Emotion

  // }
  return (
    <CustomModal onClose={props.onClose}>
      
    </CustomModal>
  );
};

export default MoreInfo;